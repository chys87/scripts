# Include this file from ~/.gitconfig:
# [include]
#     path = /path/to/this/file
# Because git-1.7 doesn't recognize push.default=simple,
# we split the file
[include]
	path = gitconfig-compat
[push]
	default = simple
